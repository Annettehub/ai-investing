# Paused Backup

这个文件夹保存暂时移出主循环的内容。

移动原因：当前系统入口过多、命令过多、维护层过重，导致知识闭环没有稳定跑通。为了先跑通 H1.2 存储周期小循环，以下内容先暂停：

- `00-system/`
- 旧 `01-commands/`
- 旧 `05-meta/`
- `docs/`
- `tools/`
- `value-investing/`
- 非当前的 `04-output` 历史输出

这些内容不是删除，只是暂停。等 H1.2 小循环连续跑通 2-3 次后，再决定哪些恢复到主目录。
