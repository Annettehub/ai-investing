# collect

目标：从 `03-raw/` 找到和当前 H1.2 小循环有关的新材料。

## 当前只收集

- HBM 产能、长约、认证、稼动率。
- HBM4 / HBM4e 单 GPU 价值量。
- DDR5 产能被 HBM 挤占。
- NAND / SSD 是否被 AI 服务器拉动。
- SK Hynix、Micron、Samsung、TSMC 与 HBM 供应链关系。

## 输出格式

```text
候选材料：
1. 文件：
   相关问题：
   支持/反驳：
   是否进入 distill：
```

不相关材料只保留在 `03-raw/`，不处理。
