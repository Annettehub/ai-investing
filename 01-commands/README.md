# MVP Commands

当前只保留三个动作，避免系统再次膨胀。

| 命令 | 目的 | 输出 |
| --- | --- | --- |
| `collect` | 从 `03-raw/` 找和 G2（原 H1.2）有关的材料 | 候选材料清单 |
| `distill` | 把候选材料沉淀进知识库 | source 卡片、concept 更新、G2 更新 |
| `weekly` | 每周复盘 G2 | `04-output/weekly/` |

旧命令已移到 `99-backup/paused-until-h1-2-loop-runs-2026-07-11/01-commands/`。
