{
  "name": "ai-investing-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run generate && astro dev",
    "generate": "python ../scripts/generate_site_content.py",
    "build": "npm run generate && astro build",
    "preview": "npm run generate && astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.38.1",
    "astro": "^6.0.4",
    "starlight-theme-rose-pine": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
